package misc;

/**
 * Given an array A of positive integers. Convert it to a sorted array with minimum cost. The only
 * valid operation are:
 * 
 * 1) Decrement with cost = 1
 * 
 * 2) Delete an element completely from the array with cost = value of element
 */

public class OptimalCost {
    int find(int[] a, int i) {
        if (i == 0) {
            return 0;
        }
        
        if (i == 1) {
            
        }
            
    }
}
