package misc;

class Node {
    int val;
    Node next;
}

public class AddLists {
    Node add(Node p, Node q) {
        if (p == null) return q;
        if (q == null) return p;
        
       int lenP = count(p);
       int lenq = count(q);
       
       
    }
}
