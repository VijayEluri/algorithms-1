package misc;

/**
 * Write a function for retrieving the total number of substring palindromes. For example the input
 * is 'abba' then the possible palindromes= a, b, b, a, bb, abba So the result is 6
 */

public class NSubstringPalindrome {
    boolean[][] palindrome;
    
    long count(String s) {
        for (int c = 0; c < s.length(); i++) {
            
        }
    }
}
